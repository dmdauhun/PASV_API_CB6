const { expect } = require("chai");
//import { expect } from 'chai';
require('colors');



describe('Math simple 1', () => {
    it('verify sum', () => {
        expect(4+4).eq(8)
    });
});
describe('Math simple 2', () => {
    it('verify sum', () => {
        expect(4+4).eq(8)
    });
});